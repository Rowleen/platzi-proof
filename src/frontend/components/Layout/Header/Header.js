import React from "react";

import styles from "styles/components/header.styl";

const Header = () => {
  return <header className={styles.header}>Desarrollador con amor</header>;
};

export default Header;
